{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@10.12.1 --activate && pnpm install --dir web --frozen-lockfile",
  "buildCommand": "pnpm --dir web build",
  "outputDirectory": "web/.next"
}
