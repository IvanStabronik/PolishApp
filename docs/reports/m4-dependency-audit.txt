# M4 dependency / security audit record

Captured in CI via `pnpm audit --prod --audit-level=high` (see workflow artifact).
Local placeholder — replace with CI output when available.

```
status: pending-ci
command: pnpm audit --prod --audit-level=high
policy: fail CI on high/critical production dependency findings
```
